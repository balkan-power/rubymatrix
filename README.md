# rubymatrix
Matrix CLI program written in Ruby.

## Usage
### Run by using
> ruby matrix.rb
### Run with different arguments
> ruby matrix.rb -[argument]
### Arguments:
- -C [colour]
- -d [delay]
- -h (help)
